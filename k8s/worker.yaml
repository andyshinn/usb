apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml
    kompose.version: 1.19.0 (f63a961c)
  creationTimestamp: null
  labels:
    io.kompose.service: worker
  name: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: worker
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml
        kompose.version: 1.19.0 (f63a961c)
      creationTimestamp: null
      labels:
        io.kompose.service: worker
    spec:
      containers:
      - args: ["celery", "-A", "usb.tasks", "worker"]
        image: usb
        env:
          - name: APPSEARCH_PRIVATE_KEY
            value: private-53spr5yxryvos8w3fo3upnrs
          - name: SENTRY_DSN
            valueFrom:
              secretKeyRef:
                name: sentry
                key: dsn
        name: worker
        resources: {}
        volumeMounts:
          - mountPath: /videos
            name: videos
          - mountPath: /thumbnails
            name: thumbnails
            readOnly: false
      restartPolicy: Always
      volumes:
        - name: videos
          hostPath:
            path: /videos
            type: Directory
        - name: thumbnails
          persistentVolumeClaim:
            claimName: thumbnails
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: thumbnails
  name: thumbnails
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
